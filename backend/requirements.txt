# Dependencias de execucao da base nova (arquitetura-alvo, secao 2).
# Conjunto declarado, editado a mao. O conjunto RESOLVIDO — o que de fato roda
# no conteiner e na integracao continua — esta em `requirements.lock`.

Django>=5.2,<6.0
django-ninja>=1.3,<2.0
psycopg[binary]>=3.2,<4.0
whitenoise>=6.7,<7.0
django-csp>=4.0,<5.0
gunicorn>=23.0,<24.0
